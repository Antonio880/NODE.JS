<!DOCTYPE html>

    <%- include('../parts/head'); %>

    <html lang="en">
    <body class="container">

        <%- include('../parts/header', {pagina: 'Home'}); %>

        <main>
            <div class="jumbotron">
                <h1>Node com EJS</h1>
                <p>Aprendendo a usar EJS</p>
                <p><%= subtitle %></p><!--Coloca-se o sinal de igualdade para poder pegar o conteúdo do objeto ou do que esta escrevendo-->
            </div>
            <ul>
                <% qualitys.forEach((quality) => { %>
                    <li>
                        <strong><%=quality.title%></strong> <%=quality.message%>
                    </li>
                <% }); %>
            </ul>
        </main>

        <%- include('../parts/footer'); %>
        
    </body>
</html>